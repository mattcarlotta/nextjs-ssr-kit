import styled from "@emotion/styled";

const ErrorMessage = styled.div`
  font-size: 32px;
  font-weight: bold;
  margin-top: -5px;
  margin-bottom: 20px;
  letter-spacing: 2px;
`;

export default ErrorMessage;
