/* istanbul ignore file */
import styled from "@emotion/styled";

const Menu = styled.div`
  list-style: none;
  margin: 0;
  padding: 0;
  outline: none;
  zoom: 1;
  display: flex;
`;

export default Menu;
